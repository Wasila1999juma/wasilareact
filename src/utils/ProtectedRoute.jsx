// import React from 'react'
// import { Navigate, json } from 'react-router-dom'

// export default function ProtectedRoute({children}) {
//       const userFound = JSON.parse(localStorage.getItem("token"))

//      if (userFound != null) {
//              if (userFound.userId) {
//                 return children;
//            } return (<Navigate to={"/"} replace />)
//      }return (<Navigate to={"/"} replace />)
// }
